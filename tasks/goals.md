# Goals

## Complete the *Space quality and security pass

status: blocked

- Stabilize the independent-product conversion, close verified security and correctness gaps, align executable behavior with documentation, propagate inherited fixes, and verify each changed repository honestly.
- All P0 and P1 work is implemented and verified locally: `bun run test:coverage` at 97.86/95.13/97.96/98.42 against a 95 floor, `bun run check` clean across 1,634 files. Publication safety audited separately — no credential is introduced by the branch, and the local paths and third-party identifiers the branch would have published are scrubbed.
- Blocked on access, not on work. The remaining acceptance criterion is squash-merging PR #6 into `starspacegroup/starspace-group-nebulakit` and verifying remote state, and `donaldfilimon` holds `pull` only on that repo — confirmed empirically by `git push --dry-run origin main`, which returns `Permission to starspacegroup/starspace-group-nebulakit.git denied to donaldfilimon` (403). Its CI has never run either: the fork-PR approval gate parked the workflow at `action_required` with zero jobs. Both need a maintainer of the org repo. The branch is merged into local `main` and pushed to the fork; only publication remains.
- Every local gate now passes against the current tree, none resting on earlier evidence: `check` 0 errors across 1,634 files, coverage 97.86/95.13/97.96/98.42 against a 95 floor, e2e 8/8, contrast both themes, `build:ci` compiled, and `prettier --check .` clean repository-wide for the first time. A full multi-agent review of PR #6 produced one finding above threshold (the `0010_` migration collision, fixed) and one below it that was real and fixed anyway (`isSuperAdmin` dropped from the consolidated admin guards).
- Not claimed as passing, per the ledger's own rule: DAST (no HawkScan runtime or key on this host) and any production build or live binding verification (`wrangler.toml` ships placeholders by design).
- Blocker re-verified on 2026-08-08, not carried over from the earlier entry: `git push --dry-run origin main` still returns `Permission to starspacegroup/starspace-group-nebulakit.git denied to donaldfilimon` (403). Delivery state is otherwise current — PR #6's head `fork/cursor/starspace-group-quality-pass-20260808`, local `main`, and the working branch are all at `776d84f` with a clean tree, so nothing is waiting to be pushed. The acceptance criterion is unchanged and still needs a maintainer of the org repo.
- Agent guidance realigned with executable behavior on 2026-08-09, which is gate-independent and so advanced while the merge stays blocked. `CLAUDE.md` predated the opaque-sessions merge (`db30a29`, `c7040ab`) and carried four false statements: a migration inventory one file behind in three places, and an auth paragraph describing the retired signed-cookie scheme rather than the opaque token whose payload lives in `sessions.data`. All four fixed, plus two previously undocumented invariants recorded (the `datetime(expires_at)` expiry normalization and the two coexisting session APIs in `db.ts`). The re-verification also confirmed a claim rather than only correcting them: the per-request privilege refresh survived the merge, so `authHandler` still satisfies the AGENTS.md Security Boundaries rule. Docs only — Prettier and `git diff --check` clean, `product-identity` and `agent-readiness` 53/53; no coverage or e2e claim is made because nothing under coverage changed.
- That correction is delivered, not just local: `46a0247`, pushed to PR #6's head branch as a fast-forward, so PR #6 reports `MERGEABLE` / `CLEAN` at that head and is 36 commits ahead of `origin/main`. Delivery to the fork is possible; publication to the org repo is not.
- Blocker re-verified again on 2026-08-09, not carried over: `git push --dry-run origin main` still returns `Permission to starspacegroup/starspace-group-nebulakit.git denied to donaldfilimon` (403). Acceptance still needs a maintainer of the org repo to squash-merge PR #6 and approve the workflow run parked at `action_required`. PR #7 (`security/opaque-sessions`) overlaps PR #6 and its merge order is a maintainer decision too.
- **The sibling audit's read-only half is now complete — 7 of 7 P0 items**, finished on 2026-08-09 with the three that had been deferred. Sanitize-before-`{@html}` and one-time OAuth state show no gap in any sibling, each verified across every call site rather than sampled; one first-pass "missing expiry check" in sortalizer was a truncated-grep false positive and is recorded as such. **Second open finding, nabu only:** its `vite.config.ts` excludes `src/hooks.server.ts` from coverage, and since nabu's `handle` is `sequence(authHandler)` that file is its entire authorization boundary. A unit test exists but always supplies a DB, so the fail-closed `!db` branch is exercised by nothing and unmeasured by the 95% floor. Below the Guides escalation in severity — no live privilege gap — but the gate that would catch one is off. Fix belongs in a nabu branch; no sibling file was edited. Propagation of both findings still waits on PR #6, so that ledger item stays `[~]`.
- Delivery vehicle changed on 2026-08-10 (sixth session): the branch `consolidate/local-main-20260810` consolidates local `main` and is PR [#9](https://github.com/starspacegroup/starspace-group-nebulakit/pull/9) (42 commits, head `8443b8b`, base `main`, `MERGEABLE`/`CLEAN`). The heads of PR #6 (`46a0247`), PR #7 (`security/opaque-sessions`), and PR #8 (`cms-v2-embeds`) are all ancestors of that head, verified with `git merge-base --is-ancestor` — merging #9 strictly contains all three. **This supersedes the "CI has never run" statements above:** a maintainer approved the fork-PR runs, and the workflow has now executed on the org repo — both jobs green on PR #9's head (run `31439300377`, 2026-08-10) and earlier on PR #7 (run `31262364171`). PR #6 now runs and **fails** both jobs at `bun install --frozen-lockfile` because its head still carries the canary-written `bun.lock` (`lockfileVersion: 2`); the fix is `e3b87b9` on PR #9's branch, so the failure is evidence for merging #9, not a regression. The session also fixed nine Codex review findings on #9 and an E2E command-palette hydration race (`8443b8b`).
- Blocker re-verified on 2026-08-10, not carried over: `git push --dry-run origin main` still returns `Permission to starspacegroup/starspace-group-nebulakit.git denied to donaldfilimon` (403). Acceptance now reads: a maintainer squash-merges PR #9 (which contains #6, #7, and #8) and closes the superseded PRs. Hosted evidence finally exists for that head — CI green is no longer a local-only claim.
- Earlier sibling-audit progress, for context: its read-only half is gate-independent, so it was run for 4 of the 7 P0 items; propagating any fix still waits on the merge, and no sibling file was staged. `isSuperAdmin` does not apply (absent from all three siblings), admin-users PII masking is already present in Guides and sortalizer and unnecessary in nabu (stricter owner-only gate), and reset plus auth-key administration are owner-gated everywhere. **One real gap found:** Guides alone lets any admin promote or delete users via `admin/users/[id]`, where *Space (`924a5ac`), nabu, and sortalizer all require owner. Evidence and false-positive checks are in `tasks/todo.md`; the fix belongs in a Guides branch, not here.
